{
  "name": "theme-engine",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "One source of truth (a VSCode theme) projected into every tool's native format.",
  "bin": {
    "theme": "./src/cli.ts"
  },
  "scripts": {
    "check": "bun run src/cli.ts check",
    "test": "bun test"
  },
  "dependencies": {
    "json5": "^2.2.3",
    "plist": "^3.1.0"
  },
  "devDependencies": {
    "shiki": "^4.2.0"
  }
}
